@*
<auto-generated>
This code was generated by Drogecode.Knrm.Oefenrooster.CodeGenerator.
If required, update the tool; not this file.

Changes to this file may cause incorrect behavior and will be lost if the code is regenerated.
</auto-generated>
*@

@if (_userRole?.Role is not null)
{
    <MudPaper Class="pa-4" MaxWidth="800px">
        @if (!_editName && !string.IsNullOrWhiteSpace(_userRole.Role.Name))
        {
            <MudText Typo="Typo.h5" Class="cursor-pointer" @onclick="() => _editName = true">@_userRole.Role.Name</MudText>
        }
        else
        {
            <MudTextField T="string?" @bind-Value="_userRole.Role.Name" Label="@L["Name"]"/>
        }
        <MudTextField T="string?" @bind-Value="_userRole.Role.ExternalId" Counter="@DefaultSettingsHelper.MAX_LENGTH_ROLE_EXTERNAL_ID" MaxLength="@DefaultSettingsHelper.MAX_LENGTH_ROLE_EXTERNAL_ID" Label="@L["ExternalId"]"/>
        <MudText Typo="Typo.h6">@L["Global"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["basic_access"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_basic_access" ValueChanged="@(isChecked => { _userRole.Role.AUTH_basic_access = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["basic_access"]</MudText><MudText Typo="Typo.body2">@L["basic_access-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["showCfooter"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_show_c_footer" ValueChanged="@(isChecked => { _userRole.Role.AUTH_show_c_footer = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["showCfooter"]</MudText><MudText Typo="Typo.body2">@L["showCfooter-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["Configuration"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_global_all"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_global_all" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_global_all = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_global_all"]</MudText><MudText Typo="Typo.body2">@L["configure_global_all-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_training-types"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_training_types" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_training_types = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_training-types"]</MudText><MudText Typo="Typo.body2">@L["configure_training-types-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_user_roles"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_user_roles" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_user_roles = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_user_roles"]</MudText><MudText Typo="Typo.body2">@L["configure_user_roles-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_user_functions"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_user_functions" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_user_functions = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_user_functions"]</MudText><MudText Typo="Typo.body2">@L["configure_user_functions-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_default_schedule"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_default_schedule" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_default_schedule = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_default_schedule"]</MudText><MudText Typo="Typo.body2">@L["configure_default_schedule-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["configure_vehicles"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_configure_vehicles" ValueChanged="@(isChecked => { _userRole.Role.AUTH_configure_vehicles = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["configure_vehicles"]</MudText><MudText Typo="Typo.body2">@L["configure_vehicles-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["Scheduler"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler"]</MudText><MudText Typo="Typo.body2">@L["scheduler-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_delete"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_delete" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_delete = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_delete"]</MudText><MudText Typo="Typo.body2">@L["scheduler_delete-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_delay_sync"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_delay_sync" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_delay_sync = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_delay_sync"]</MudText><MudText Typo="Typo.body2">@L["scheduler_delay_sync-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_table"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_in_table_view" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_in_table_view = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_table"]</MudText><MudText Typo="Typo.body2">@L["scheduler_table-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_history"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_history" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_history = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_history"]</MudText><MudText Typo="Typo.body2">@L["scheduler_history-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_past"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_edit_past" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_edit_past = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_past"]</MudText><MudText Typo="Typo.body2">@L["scheduler_past-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_dayitem"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_dayitem" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_dayitem = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_dayitem"]</MudText><MudText Typo="Typo.body2">@L["scheduler_dayitem-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_monthitem"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_monthitem" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_monthitem = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_monthitem"]</MudText><MudText Typo="Typo.body2">@L["scheduler_monthitem-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_other"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_other_user" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_other_user = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_other"]</MudText><MudText Typo="Typo.body2">@L["scheduler_other-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_remaining_unavailable"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_remaining_days_unavailable" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_remaining_days_unavailable = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_remaining_unavailable"]</MudText><MudText Typo="Typo.body2">@L["scheduler_remaining_unavailable-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_description_edit"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_description_edit" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_description_edit = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_description_edit"]</MudText><MudText Typo="Typo.body2">@L["scheduler_description_edit-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["scheduler_description_read"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_scheduler_description_read" ValueChanged="@(isChecked => { _userRole.Role.AUTH_scheduler_description_read = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["scheduler_description_read"]</MudText><MudText Typo="Typo.body2">@L["scheduler_description_read-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["User"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["users_details"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_users_details" ValueChanged="@(isChecked => { _userRole.Role.AUTH_users_details = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["users_details"]</MudText><MudText Typo="Typo.body2">@L["users_details-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["users_delete"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_users_delete" ValueChanged="@(isChecked => { _userRole.Role.AUTH_users_delete = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["users_delete"]</MudText><MudText Typo="Typo.body2">@L["users_delete-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["users_counter"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_users_counter" ValueChanged="@(isChecked => { _userRole.Role.AUTH_users_counter = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["users_counter"]</MudText><MudText Typo="Typo.body2">@L["users_counter-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["users_settings"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_users_settigns" ValueChanged="@(isChecked => { _userRole.Role.AUTH_users_settigns = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["users_settings"]</MudText><MudText Typo="Typo.body2">@L["users_settings-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["Dashboard"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["full_action_history"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_action_history_full" ValueChanged="@(isChecked => { _userRole.Role.AUTH_action_history_full = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["full_action_history"]</MudText><MudText Typo="Typo.body2">@L["full_action_history-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["full_training_history"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_training_history_full" ValueChanged="@(isChecked => { _userRole.Role.AUTH_training_history_full = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["full_training_history"]</MudText><MudText Typo="Typo.body2">@L["full_training_history-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["action_search"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_action_search" ValueChanged="@(isChecked => { _userRole.Role.AUTH_action_search = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["action_search"]</MudText><MudText Typo="Typo.body2">@L["action_search-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["action_share"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_action_share" ValueChanged="@(isChecked => { _userRole.Role.AUTH_action_share = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["action_share"]</MudText><MudText Typo="Typo.body2">@L["action_share-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["full_dashboard_statistics"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_dashboard_Statistics" ValueChanged="@(isChecked => { _userRole.Role.AUTH_dashboard_Statistics = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["full_dashboard_statistics"]</MudText><MudText Typo="Typo.body2">@L["full_dashboard_statistics-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["full_dashboard_statistics_ut"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_dashboard_Statistics_user_tabel" ValueChanged="@(isChecked => { _userRole.Role.AUTH_dashboard_Statistics_user_tabel = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["full_dashboard_statistics_ut"]</MudText><MudText Typo="Typo.body2">@L["full_dashboard_statistics_ut-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["dashboard_hol"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_dashboard_holidays" ValueChanged="@(isChecked => { _userRole.Role.AUTH_dashboard_holidays = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["dashboard_hol"]</MudText><MudText Typo="Typo.body2">@L["dashboard_hol-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["Mail"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["mail_invite_external"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_mail_invite_external" ValueChanged="@(isChecked => { _userRole.Role.AUTH_mail_invite_external = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["mail_invite_external"]</MudText><MudText Typo="Typo.body2">@L["mail_invite_external-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudText Typo="Typo.h6">@L["PreCom"]</MudText>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["precom_problems"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_precom_problems" ValueChanged="@(isChecked => { _userRole.Role.AUTH_precom_problems = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["precom_problems"]</MudText><MudText Typo="Typo.body2">@L["precom_problems-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["precom_manual"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_precom_manual" ValueChanged="@(isChecked => { _userRole.Role.AUTH_precom_manual = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["precom_manual"]</MudText><MudText Typo="Typo.body2">@L["precom_manual-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <div>
            <MudTooltip><ChildContent>
                <MudSwitch Label="@L["precom_sync_calendar"]" Color="Color.Primary" T="bool" Value="@_userRole.Role.AUTH_precom_sync_calendar" ValueChanged="@(isChecked => { _userRole.Role.AUTH_precom_sync_calendar = isChecked; _saved = null; })"/>
            </ChildContent><TooltipContent><MudText Typo="Typo.h6">@L["precom_sync_calendar"]</MudText><MudText Typo="Typo.body2">@L["precom_sync_calendar-body"]</MudText></TooltipContent></MudTooltip>
        </div>
        <MudButton Color="Color.Primary" OnClick="Submit">
            @LApp["Submit"]
            @switch (_saved)
            {
                case true:
                    <MudIcon Icon="@Icons.Material.Filled.Check"/>
                    break;
                case false:
                    <MudIcon Icon="@Icons.Material.Filled.Close"/>
                    break;
            }
        </MudButton>
    </MudPaper>
}
else if (_userRole is null)
{
    <DrogeLoading />
}
